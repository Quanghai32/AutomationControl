<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:PrismTest"
                    xmlns:MachineModel="clr-namespace:MachineModel;assembly=MachineModel"
                    >

    <DataTemplate DataType="{x:Type local:KeyenceMachineViewModel}">
        <local:MachineViewSmall/>
        <!--<StackPanel>
            <TextBlock Text="{Binding Name}"/>
            <TextBlock Text="{Binding Actual.MemoryValue}"/>
        </StackPanel>-->
    </DataTemplate>
</ResourceDictionary>